# openclaw

A new Flutter project.

## local build

```bash
# clean cache
flutter clean

# Retrieve dependencies again
flutter pub get

# check error
flutter doctor -v

# build apk
flutter build apk --release --split-per-abi
```